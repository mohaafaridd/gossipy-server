# Migration `20200417085329-v3`

This migration has been generated by Mohammed Farid at 4/17/2020, 8:53:29 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "prod"."Comment" ALTER COLUMN "image" DROP NOT NULL,
ALTER COLUMN "image" SET DEFAULT '';

ALTER TABLE "prod"."Station" ALTER COLUMN "image" DROP NOT NULL,
ALTER COLUMN "image" SET DEFAULT '';

ALTER TABLE "prod"."Topic" ALTER COLUMN "image" DROP NOT NULL,
ALTER COLUMN "image" SET DEFAULT '';

ALTER TABLE "prod"."User" ALTER COLUMN "image" DROP NOT NULL,
ALTER COLUMN "image" SET DEFAULT '';
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200417071232-v2..20200417085329-v3
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("SCHEMA_URL")
 }
 // output   = "../generated"
 generator client {
@@ -10,9 +10,9 @@
 model User {
   id          Int          @default(autoincrement()) @id
   name        String
-  image       String?
+  image       String?      @default("")
   identifier  String       @unique
   email       String       @unique
   password    String
   stations    Station[]    @relation(references: [id])
@@ -26,9 +26,9 @@
 model Station {
   id          Int          @default(autoincrement()) @id
   name        String
-  image       String?
+  image       String?      @default("")
   identifier  String       @unique
   description String
   users       User[]       @relation(references: [id])
   memberships Membership[]
@@ -44,9 +44,9 @@
   id           Int        @default(autoincrement()) @id
   title        String
   identifier   String
   content      String
-  image        String?
+  image        String?    @default("")
   sealed       Boolean    @default(false)
   membershipId Int
   userId       Int
   stationId    Int
@@ -61,9 +61,9 @@
 model Comment {
   id           Int        @default(autoincrement()) @id
   content      String
-  image        String?
+  image        String?    @default("")
   membershipId Int
   userId       Int
   topicId      Int
   stationId    Int
```


